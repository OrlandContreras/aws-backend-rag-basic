# Configuración base compartida por todos los entornos
# Este archivo es un ejemplo, crea tu propio .env con los valores correctos

# URL de la API por defecto (fallback)
VITE_API_URL=/api/agent

# Otras variables globales pueden agregarse aquí
VITE_APP_NAME=Chat con Agente IA
VITE_APP_VERSION=1.0.0 